image_root: '/project_data/datasets/nocaps/'
ann_root: 'annotation'

# set pretrained as a file path or an url
pretrained: '/home/sachingo/BLIP/output_new/blip_laion_16M_tmars_clipgrad_stage2/checkpoint_3000.pth'

vit: 'base'
batch_size: 32

image_size: 384

max_length: 20
min_length: 5
num_beams: 3
prompt: 'a picture of '